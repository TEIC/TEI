1. Restore @generate on <schemaSpec>
2. Remove @xml:space from att.global
3. generate data.name not xsd:token for semi-open valLists
4. Move @xml:base into SA
5. Add generic segment boundary element or licence use of <milestone>
for same in SA
6. Add 
   <!ENTITY % xsi-schemaloc-def "xsi:schemaLocation CDATA #IMPLIED">
   <!ENTITY % xmlns-xsi-def "xmlns:xsi CDATA #FIXED
   'http://www.w3.org/2001/XMLSchema-instance'"> 
   when generating DTDs. And other schemas too.

-----------

MS: no example is sourced
PH: replace with unencumbered images