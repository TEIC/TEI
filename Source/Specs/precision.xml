<?xml version="1.0" encoding="utf-8"?>
<elementSpec xmlns="http://www.tei-c.org/ns/1.0" xmlns:rng="http://relaxng.org/ns/structure/1.0" module="certainty" ident="precision">
  <desc>indicates the numerical accuracy or precision  associated
  with some aspect of the text markup. </desc>
  <classes>
    <memberOf key="model.global.meta"/>
  </classes>
  <content>
    <rng:zeroOrMore>
      <rng:ref name="model.glossLike"/>
    </rng:zeroOrMore>
  </content>
  <attList>
    <attDef ident="target" usage="opt">
      <equiv/>
      <desc>points at the element or elements the precision of which
      is to be specified.</desc>
      <datatype maxOccurs="unbounded">
        <rng:ref name="data.pointer"/>
      </datatype>
      <remarks>
        <p>If more than one identifier is given, the
        <gi>precision</gi> element is interpreted as applying to all.</p>
      </remarks>
    </attDef>

    <attDef ident="pattern" usage="opt">
      <equiv/>
      <desc>supplies an XSLT 2.0 pattern which may be used to select
      those portions of the
      document the precision of which is to be specified.</desc>
      <datatype>
        <rng:text/>
      </datatype>
<valDesc>The pattern should be expressed using the pattern syntax
defined in <ptr target="#XSLT2"/>. </valDesc>
      <remarks>
        <p>The context for the pattern is the nodeset identified by
        the value of the <att>target</att> attribute. If no value is
        given for <att>target</att>, the context is the document
        root element.</p>
<p>Note that the value of the <att>target</att> attribute may include
an XPointer expression which may include an XPath expression (see <ptr
target="#SATS"/>). Either method may be used to identify the parts of
a document whose precision is to be documented by this element.</p>
      </remarks>
    </attDef>

    <attDef ident="degree" usage="opt">
      <equiv/>
      <desc>indicates the degree of precision to be assigned as a
      value between 0 (none) and 1 (optimally precise)</desc>
      <datatype>
        <rng:ref name="data.probability"/>
      </datatype>
    </attDef>


    <attDef ident="stdDeviation" usage="opt">
      <equiv/>
      <desc>supplies a standard deviation associated with the value in
      question</desc>
      <datatype>
        <rng:ref name="data.numeric"/>
      </datatype>
    </attDef>
  </attList>

</elementSpec>
