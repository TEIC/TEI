<html>
<head>
<title>Roma: generating validators for the TEI</title>
<link rel="stylesheet" type="text/css" href="roma.css">
<script src="roma.js" type="text/javascript"></script>
</head>
<body>
<table borde="0" cellspacing="0" cellpadding="0" width="100%">
 <tr>
  <td align="left"><p class="headline"><a href="index.html">Roma</a>: generating validators for the TEI</p></td>
  <td align=right">
    <table border="0" width="100%">
      <td align="right"><p class="smallHeadline">Text Encoding
      Initiative</p></td>
      </table>
  </td>
 </tr>
</table>
<br/>
<table class="topLinks">
  <tr>
   <td{if:view=customizeCustomization} class="selected"{endif}><a
   href="?mode=customizeCustomization">Customize Customization</a></td>
   <td{if:view=main} class="selected"{endif}><a href="?mode=main">Change Modules</a></td>
   <td{if:view=listElements} class="selected"{endif}><a href="?mode=listAddedElements">Add Elements</a></td>
   <td{if:view=changeClasses} class="selected"{endif}><a
   href="?mode=changeClasses">Change Classes</a></td>
   <td{if:view=customizeLanguage} class="selected"{endif}><a href="?mode=customizeLanguage">Customize Language</a></td>
   <td{if:view=createSchema} class="selected"{endif}><a href="?mode=createSchema">Create Schema</a></td>
   <td{if:view=createDocumentation} class="selected"{endif}><a href="?mode=createDocumentation">Create Documentation</a></td>
   <td{if:view=saveCustomization} class="selected"{endif}><a
   href="?mode=saveCustomization">Save Customization</a></td>
   <td{if:help=on} class="selected"{endif}><a href="?help=switch">Help</a></td>
   <td class="newCustomization"><a href="?mode=newCustomization">Create new Customization</a></td>

</tr>
 </table>

{if:help=on}
  <p class="help">{res:roma/res/$[mode]|de|helpmessage}</p>
{endif}
 <p class="siteHeadline">{res:roma/res/$[mode]|en|headline}</p>
 {notams}
 {template}
<hr noshade>
<p>CVS date: $Date$</p>
<p class="author"><a href="mailto:oss-arno@mittelbach-online.de">Arno Mittelbach</a></p>
