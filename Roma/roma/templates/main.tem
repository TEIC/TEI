<html>
<head>
<title>Roma: generating validators for the TEI</title>
<link rel="stylesheet" type="text/css" href="roma.css">
<script src="roma.js" type="text/javascript"></script>
</head>
<body>
<table class="topLinks" borde="0" cellspacing="0" cellpadding="0" width="100%">
 <tr>
  <td align="left"><p class="headline"><a href="index.html">Roma</a>:
  generating validators for the TEI</p></td>
<td class="topLinks">Search TEI database:
<form target="_new" action="/TEI/Roma/xquery/tag.xq" method="GET">
  <input name="name" width="15"/>
  <input type="submit" />
</form>

</td>
  <td align=right">
    <table border="0" width="100%">
      <td align="right"><p class="smallHeadline"><a
      href="http://www.tei-c.org" target="_blank">Text Encoding
      Initiative</a></p></td>
      </table>
  </td>
 </tr>
</table>
<br/>
<table class="topLinks">
  <tr>
   <td{if:view=saveCustomization} class="selected"{endif}><a
   href="?mode=saveCustomization">Save</a></td>
   <td{if:view=customizeCustomization} class="selected"{endif}><a
   href="?mode=customizeCustomization">Customize</a></td>
   <td><a href="?mode=newCustomization">New</a></td>
   <td{if:help=on} class="selected"{endif}><a
   href="?help=switch">Help</a></td>
<td></td>
<td></td>
</tr>
<tr>
   <td{if:view=main} class="selected"{endif}><a href="?mode=main">Modules</a></td>

   <td{if:view=listElements} class="selected"{endif}><a href="?mode=listAddedElements">Add Elements</a></td>

   <td{if:view=changeClasses} class="selected"{endif}><a
   href="?mode=changeClasses">Change Classes</a></td>

   <td{if:view=customizeLanguage} class="selected"{endif}><a href="?mode=customizeLanguage">Language</a></td>

   <td{if:view=createSchema} class="selected"{endif}><a href="?mode=createSchema">Schema</a></td>
   <td{if:view=createDocumentation} class="selected"{endif}><a href="?mode=createDocumentation">Documentation</a></td>

</tr>
 </table>
<p><b>Warning! this version of Roma uses a pre-release draft of TEI P5
 from 1st October 2004</b></p>
{if:help=on}
  <p class="help">{res:roma/res/$[mode]|$[lang]|helpmessage}</p>
{endif}
 <p class="siteHeadline">{res:roma/res/$[mode]|$[lang]|headline}</p>
 {notams}
 {template}
<hr noshade>
<p><img float="left" src="teilogo.jpg"/> 
CVS date: $Date$, $Revision$</p>
<p class="author"><a href="mailto:oss-arno@mittelbach-online.de">Arno Mittelbach</a></p>
