<?xml version="1.0" encoding="UTF-8"?>
<div type="test" xmlns="http://www.tei-c.org/ns/1.0">
  <p>Testing:
     <distinct type="one"/>
     <distinct type="two"/>
     <distinct type="invalid"/>
     <distinct type="one two"/>
     <distinct type="one invalid two"/>
     <distinct type="i little can two little"/>
     <distinct type="  i little can two little "/>
     <distinct type="  i little invalid can two "/>
     <distinct type=" i one i i two i i i three "/>
   Each of the above should be valid except those that use "invalid" in the
   value.</p>
</div>
