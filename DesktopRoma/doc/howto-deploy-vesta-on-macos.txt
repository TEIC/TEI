1) run ant
2) create .app with jar bundler (not in eclipse dir)
3) change plist file and add
       <key>StartOnMainThread</key>
       <true/>
   to the java section
4) copy resources into bundle ../Resources
5) run 'hdiutil create -srcfolder vesta vesta.dmg' to create dmg
6) copy dmg to binaries